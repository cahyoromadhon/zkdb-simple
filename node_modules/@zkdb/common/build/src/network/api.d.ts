export type TChain = 'mainnet' | 'devnet';
export type TMinaTransactionStatus = 'applied' | 'failed' | 'pending';
export type TBlockConfirmationTransaction = {
    blockConfirmationsCount: number;
    blockHeight: number;
    stateHash: string;
    txHashNext: string;
    txStatus: TMinaTransactionStatus;
    txHashPrevious: string;
    isCanonical: boolean;
    failureReason: string;
};
export type TZkAppTransaction = {
    txStatus: TMinaTransactionStatus;
    failures: string[];
};
export declare class BlockberryApi {
    #private;
    private chain;
    constructor(apiKey: string, chain: TChain);
    getBlockConfirmationByTransactionHash(txHash: string): Promise<TBlockConfirmationTransaction | undefined>;
    getZkAppTransactionByTxHash(txHash: string): Promise<TZkAppTransaction | undefined>;
}
