type TSendTransaction = {
    hash: string;
    id: string;
    zkappCommand: {
        memo: string;
    };
};
export declare function sendTransaction(json: unknown, url: string): Promise<TSendTransaction>;
export {};
