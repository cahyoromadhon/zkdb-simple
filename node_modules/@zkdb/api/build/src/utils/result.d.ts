export type TResultType = "error" | "undefined" | "data";
export type TResult<T> = T | any | undefined | null;
export declare class GraphQLResult<T> {
    private type;
    private result;
    private constructor();
    static wrap<T>(result: TResult<T>): GraphQLResult<T>;
    get length(): number;
    isError(): boolean;
    isUndefined(): boolean;
    isValid(): boolean;
    isArray(): boolean;
    isObject(): boolean;
    /**
     * Checks if the result contains exactly one object.
     */
    isOne(): boolean;
    /**
     * Checks if the result contains one or more objects.
     */
    isSome(): boolean;
    /**
     * Checks if the result contains more than one item.
     */
    isMany(): boolean;
    unwrap(): T;
}
