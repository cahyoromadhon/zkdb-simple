import { IApiClient } from '@zkdb/api';
import { TCollectionMetadata, TCollectionMetadataResponse } from '@zkdb/common';
import { OwnershipAndPermission, Permission } from '@zkdb/permission';
import { IMetadata } from '../interfaces';
export declare class CollectionMetadata implements IMetadata<TCollectionMetadata> {
    private databaseName;
    private collectionName;
    private apiClient;
    private get basicRequest();
    constructor(apiClient: IApiClient, databaseName: string, collectionName: string);
    info(): Promise<TCollectionMetadataResponse>;
    groupSet(groupName: string): Promise<boolean>;
    ownerSet(userName: string): Promise<boolean>;
    permissionSet(permission: Permission): Promise<boolean>;
    permissionGet(): Promise<OwnershipAndPermission>;
}
