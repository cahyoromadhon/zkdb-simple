import { IApiClient } from '@zkdb/api';
import { TGroupDetail } from '@zkdb/common';
import { IGroup, TGroupConfig } from '../interfaces';
/**
 * This class is an implementation of the IGroup interface.
 * It provides methods to interact with a group in a database.
 * @class Group
 * @implements {IGroup}
 */
export declare class Group implements IGroup {
    private apiClient;
    private databaseName;
    private groupName;
    private get basicQuery();
    constructor(apiClient: IApiClient, databaseName: string, groupName: string);
    exist(): Promise<boolean>;
    info(): Promise<TGroupDetail | null>;
    create(groupConfig?: Omit<TGroupConfig, 'groupName'> | undefined): Promise<boolean>;
    update(groupConfig: Partial<TGroupConfig>): Promise<boolean>;
    userAdd(listUser: string[]): Promise<boolean>;
    userRemove(listUser: string[]): Promise<boolean>;
}
