import { IApiClient } from '@zkdb/api';
import { TGroupListByUserResponse, TUser } from '@zkdb/common';
import { IUser } from '../interfaces/user';
/**
 * Class representing a user.
 * @class User
 * @implements {IUser}
 */
export declare class User implements IUser {
    private apiClient;
    private databaseName;
    private userFilter;
    constructor(apiClient: IApiClient, databaseName: string, userFilter: Partial<Pick<TUser, 'email' | 'publicKey' | 'userName'>>);
    listGroup(): Promise<TGroupListByUserResponse>;
    info(): Promise<Omit<TUser, 'userData'> | null>;
    exist(): Promise<boolean>;
}
