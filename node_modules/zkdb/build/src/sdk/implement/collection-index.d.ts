import { IApiClient } from '@zkdb/api';
import { TCollectionIndex, TIndexListResponse } from '@zkdb/common';
import { ICollectionIndex } from '../interfaces';
export declare class CollectionIndex implements ICollectionIndex {
    private databaseName;
    private collectionName;
    private apiClient;
    constructor(apiClient: IApiClient, databaseName: string, collectionName: string);
    private get basicRequest();
    list(): Promise<TIndexListResponse>;
    create(index: TCollectionIndex[]): Promise<boolean>;
    drop(indexName: string): Promise<boolean>;
}
