import { IApiClient } from '@zkdb/api';
import { IMinaProvider, TUserSignInResponse } from '@zkdb/common';
export declare const ZKDB_KEY_ACCESS_TOKEN = "accessToken";
export declare const ZKDB_KEY_USER_INFO = "userInfo";
export declare class Authenticator {
    #private;
    private apiClient;
    constructor(signer: IMinaProvider, apiClient: IApiClient, userName: string, storage?: Storage);
    private get user();
    private timestamp;
    get signer(): IMinaProvider;
    connect(signer: IMinaProvider): void;
    isLoggedIn(): boolean;
    isUserExist(userName: string): Promise<boolean>;
    signIn(): Promise<TUserSignInResponse>;
    signUp(email: string): Promise<import("@zkdb/common").TUserSignUpResponse>;
    signOut(): Promise<void>;
    getUser(): Omit<TUserSignInResponse, 'userData' | 'accessToken'> | undefined;
    getAccessToken(): string | undefined;
}
