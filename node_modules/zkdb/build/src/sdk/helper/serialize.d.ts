export type TSerializedBigInt<T> = {
    [K in keyof T]: T[K] extends bigint ? string : T[K];
};
export declare function serializeDocument<T extends Record<string, any>>(document: T): TSerializedBigInt<T>;
